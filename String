# Check if given strings are rotations of each other or not
java
class Solution {
    public boolean rotateString(String s, String s1) {
        if (s.length()==s1.length() && (s+s).contains(s1)) return true;
        return false;
    }
}
